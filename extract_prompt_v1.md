# 文档信息提取任务
---
## 任务描述
从【文件内容区】中根据“提取规则”提取字段信息，并按照“输出格式”和“输出要求”进行输出。
---
## 特别注意事项（强约束）
- “发包人”与“委托方”同义
- 金额可能为大写或数字
- 电话通常位于签署页
- “分阶段付款”字段的提取内容，一定能从原文找到，禁止输出 "unknown"
- 禁止编造数据，所有输出必须来源于原文
- 系统可能会将同一份文档拆分为多个 chunk 分别提取，最后进行跨 chunk 合并
- 当前任务仅对“本 chunk 中能够明确提取的信息”负责
- 若本 chunk 中无法提取某字段 → 输出 "unknow" 或 ""（空字符串）或 []（空数组）
---
## 输出结构
```
{
  "文件名": "",
  "工程名称": "",
  "合同编号": "",
  "签订日期": "",
  "签约金额": "",
  "发包人": "",
  "发包人电话（签署页）": "",
  "发包人联系人（签署页）": "",
  "设计范围": "",
  "项目负责人": "",
  "设计人员名单": [],
  "分阶段付款": {
    "阶段数量": "",
    "付款计划": []
  }
}
```
---
## 字段内容提取规则
### 1. **文件名**
   - 提取文档的文件名。
### 2. **工程名称**
   - 提取文档中明确标注的 "工程名称:" 后的内容。
   - 仅提取第一处出现的工程名称。
### 3. **合同编号**
   - 提取文档中明确标注的 "合同编号（发包人）:" 或 "合同编号（委托方）:" 后的编号。
   - 忽略 "合同编号（设计人）:" 或其他类型的编号。
   - 严禁提取"合同编号（设计人）/合同编号（受托方）:"后的编号。
### 4. **签订日期**
   - 提取文档中明确标注的 "签订日期:" 后的日期。
   - 日期格式统一为 "YYYY年MM月DD日"。
### 5. **签约金额**
   - 提取文档中明确标注的 "签约合同价格" 或 "合同金额" 后的金额。
   - 如果金额为人民币大写，需转换为阿拉伯数字。
   - 严禁使用设计费金额替代合同金额。
### 6. **发包人**
   - 提取文档中明确标注的 "发包人:" 或 "委托方:" 后的内容。
### 7. **发包人电话（签署页）**
   - 提取合同签署页中第一个出现的电话号码。
   - 默认第一个电话号码即为发包人或委托方。
### 8. **发包人联系人（签署页）**
   - 提取合同签署页中第一个联系人姓名。
   - 位置与发包人电话对应。
### 9. **设计范围**
   - 提取“第一部分 合同协议书”中的“设计范围”或“工程承包范围”。
   - 必须为完整内容。
   - 删除引导性语句。
### 10. **项目负责人**
   - 提取设计人员名单中职责为“项目负责人”的姓名。
### 11. **设计人员名单**
   - 仅提取人员姓名。
   - 数据来源于附件。
### 12. **分阶段付款**
   - 按照下面的权威指导步骤,完成提取:
   ---
   - (1). 输出格式:
  ```
    {
       "阶段数量": （默认值为`3`）,
       "付款计划": [
          {
             "阶段编号": 1,
             "付款比例": "（默认值为`30%`）",
             "付款依据或要求": "",
             "付款时间": "（默认值为`30日以内`）"
          },
          {
             "阶段编号": 2,
             "付款比例": "（默认值为`40%`）",
             "付款依据或要求": "",
             "付款时间": "（默认值为`30日以内`）"
          },
          {
             "阶段编号": 3,
             "付款比例": "（默认值为`30%`）",
             "付款依据或要求": "",
             "付款时间": "（默认值为`30日以内`）"
          }
       ]
    }
  ```
   - **注意**
      "分阶段付款信息",需要从 `第三部分 专用合同条款` 中提取,因此:
      若本chunk中,出现了 `# 第二部分 通用合同条款` 的文字内容,
         → 本chunk中的"分阶段付款"提取结果,**必须**全部输出空值或`null`
   ---
   - (2) 结构定位
      - 在当前chunk中：
      - STEP1：
      查找所有出现的 `12 合同价格与支付` ，其它可能的格式如下：
         - `12.合同价格与支付`
         - `12合同价格与支付`
         - `# 12.合同价格与支付`
      - STEP2：
      如果存在多个匹配：
      → 使用**最后一次出现**的位置（优先级最高）
      - STEP3：
      从该位置开始，作为**分阶段付款信息的起始位置**
      - STEP4：
      如果**当前chunk中**不存在`12 合同价格与支付`：
      → "分阶段付款" 返回空结构或`null`
   ---
   - (3) 子章节识别
      识别以下子章节（关键词匹配即可）：
      - 第一阶段：
      `12.2 定金或预付款` （关键词：定金 / 预付款）
      - 第二阶段：
      `12.3 中期支付` （关键词：中期 / 进度）
      - 第三阶段：
      `12.4 费用结算` （关键词：结算 / 尾款）
      - 特殊章节：
      `12.5 支付方式`
   ---
   - (4) 付款类型判断
      - 仅允许两种类型：
      - 类型A. 一次性付款（输出 `1 阶段`）
         - 满足以下条件：
         - 章节内容中出现 `一次性支付` / `一次付清`
         - 且没有出现 `30%`、`40%` 等比例
      - 类型B. 三阶段付款（默认）（输出 `3阶段`）
         - 满足以下任一条件：
         - 出现百分比（30%、40%等）
         - 或存在 12.2 / 12.3 / 12.4 结构
   ---
   - (5) 百分比识别
      - 以下全部视为有效：
        - 30 %
        - $30 \%$
        - $30\\%$
        - $3 0 \\%$
        - 百分之三十
      - 统一输出数值：
        - `30%`
   ---
   - (6) 特殊提取规则
      - 若 `定金或预付款` 等章节内容,出现 `不适用` 的内容
      - 则改用 `12.5 支付方式` 进行提取
   ---
   - (7) 一般提取规则（**核心**）
      - 对于每个阶段（12.2 / 12.3 / 12.4）：
      - **付款比例**字段：
         - 优先从该子章节中提取百分比
         - 若未找到，则使用默认值
      - **付款依据**字段：
         在该子章节中：
         - 查找包含以下任一特征的文本：
            - 包含`支付`
            - 或包含`付款`
            - 或包含百分比
         - 返回：
            第一个匹配到的完整句子或完整段落
      - **付款时间**字段：
         - 在**提取到百分比的章节**中,提取包含时间描述的内容
         - 若未找到，则使用默认值
---
